<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Handle trailing slashes and html extensions for Next.js static export
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME}\.html -f
  RewriteRule ^(.*)$ $1.html [L]
</IfModule>
